###############################################
# Do not Edit! Generated by:
# spec-builder version 0.13-prerelease
###############################################

Name: cairo
Summary: A vector graphics library
Group: System Environment/Libraries
Version: 1.8.6
License: LGPLv2 or MPLv1.1
URL: http://www.cairographics.org
Release: 1%{?dist}
Source0: http://cairographics.org/releases/%{name}-%{version}.tar.gz
BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}-root-%(%{__id_u} -n)

BuildRequires: pkgconfig(xrender)
BuildRequires: pkgconfig(x11)
BuildRequires: pkgconfig(libpng)
BuildRequires: pkgconfig(libxml-2.0)
BuildRequires: pkgconfig(pixman-1)
BuildRequires: pkgconfig(freetype2)
BuildRequires: pkgconfig(fontconfig)


%description
Description: %{summary}

%package devel

Summary: Development components for the cairo library
Group: Development/Libraries

Requires: %{name} >= %{version}

%description devel
Description: %{summary}

%prep
%setup -q -n %{name}-%{version}

%build
%configure --enable-warnings --enable-xlib --enable-freetype --enable-ps --enable-pdf --enable-svg --disable-gtk-doc --disable-static
make %{?_smp_mflags}

%install
rm -rf %{buildroot}
%make_install

%find_lang cairo-0.10 || echo -n >> cairo-0.10.lang

mkdir -p %{buildroot}/%{_datadir}/doc/%{name}-%{version}
for f in `ls %{buildroot}/%{_datadir}/doc/`; do
	if [ -f %{buildroot}/%{_datadir}/doc/$f ]; then
		mv %{buildroot}/%{_datadir}/doc/$f %{buildroot}/%{_datadir}/doc/%{name}-%{version}
	fi
done

%clean
rm -rf %{buildroot}

%post -p /sbin/ldconfig

%postun -p /sbin/ldconfig

%post devel -p /sbin/ldconfig

%postun devel -p /sbin/ldconfig

%files -f cairo-0.10.lang
%defattr(-,root,root,-)
%doc AUTHORS BIBLIOGRAPHY BUGS ChangeLog COPYING COPYING-LGPL-2.1 COPYING-MPL-1.1 NEWS PORTING_GUIDE README
%{_libdir}/libcairo*.so.* 

%files devel
%defattr(-,root,root,-)
%{_includedir}/*
%{_libdir}/libcairo*.so
%{_libdir}/pkgconfig/*
%{_datadir}/gtk-doc/html/cairo


